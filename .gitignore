node_modules
dist
dist-ssr
.vite
*.local
.DS_Store
.env
.env.*
!.env.example

# TypeScript incremental build state
*.tsbuildinfo

# Persönliche Editor-Einstellungen. Ein Hook darin zeigt auf einen absoluten
# Pfad dieses Rechners und wäre auf jedem anderen kaputt.
.claude/settings.local.json

# Design tool working files. The surface brief under surfaces/ is durable
# strategy and is committed; the rest is per-run evidence.
.impeccable/review/
.impeccable/mocks/
.impeccable/build/
.impeccable/live/

# Desktop build output (npm run build:desktop)
release/
desktop/node_modules/
desktop/.stage/
desktop/.build/
desktop/.site/
